/**
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech) (7.8.0).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */
package org.smartbit4all.api.navigation.restserver;

import org.smartbit4all.api.navigation.bean.NavigationEntry;
import org.smartbit4all.api.navigation.bean.NavigationResponse;
import java.net.URI;
import io.swagger.v3.oas.annotations.ExternalDocumentation;
import io.swagger.v3.oas.annotations.Operation;
import io.swagger.v3.oas.annotations.Parameter;
import io.swagger.v3.oas.annotations.Parameters;
import io.swagger.v3.oas.annotations.media.ArraySchema;
import io.swagger.v3.oas.annotations.media.Content;
import io.swagger.v3.oas.annotations.media.Schema;
import io.swagger.v3.oas.annotations.responses.ApiResponse;
import io.swagger.v3.oas.annotations.security.SecurityRequirement;
import io.swagger.v3.oas.annotations.tags.Tag;
import io.swagger.v3.oas.annotations.enums.ParameterIn;
import org.springframework.http.ResponseEntity;
import org.springframework.validation.annotation.Validated;
import org.springframework.web.bind.annotation.*;
import org.springframework.web.multipart.MultipartFile;

import jakarta.validation.Valid;
import jakarta.validation.constraints.*;
import java.util.List;
import java.util.Map;
import jakarta.annotation.Generated;

@Generated(value = "org.openapitools.codegen.languages.SpringCodegen", comments = "Generator version: 7.8.0")
@Validated
@Tag(name = "Navigation", description = "the Navigation API")
public interface NavigationApi {

    default NavigationApiDelegate getDelegate() {
        return new NavigationApiDelegate() {};
    }

    /**
     * POST /entry : Retrieve the entries from the navigations.
     *
     * @param entryMetaUri  (required)
     * @param objectUri  (required)
     * @return The navigation entry if we found it or null if missing (status code 200)
     */
    @Operation(
        operationId = "getEntry",
        summary = "Retrieve the entries from the navigations.",
        tags = { "Navigation" },
        responses = {
            @ApiResponse(responseCode = "200", description = "The navigation entry if we found it or null if missing", content = {
                @Content(mediaType = "application/json", schema = @Schema(implementation = NavigationEntry.class))
            })
        }
    )
    @RequestMapping(
        method = RequestMethod.POST,
        value = "/entry",
        produces = { "application/json" },
        consumes = { "application/x-www-form-urlencoded" }
    )
    
    default ResponseEntity<NavigationEntry> getEntry(
        @Parameter(name = "entryMetaUri", description = "", required = true) @Valid @RequestParam(value = "entryMetaUri", required = true) URI entryMetaUri,
        @Parameter(name = "objectUri", description = "", required = true) @Valid @RequestParam(value = "objectUri", required = true) URI objectUri
    ) throws Exception {
        return getDelegate().getEntry(entryMetaUri, objectUri);
    }


    /**
     * POST /navigate : Queries all data sources to populate the associations starting from the given entry.
     *
     * @param objectUri The URI of the api object that is the starting point of the navigation. It must be a valid URI that can be the starting point of the associations we provided. (required)
     * @param associationMetaUris The list of associations to identify the direction we want to navigate. If we skip this parameter (null) then we will have all the associations defined in the meta (optional)
     * @return The map of the references by the URI of association meta we passed in the associations parameter. (status code 200)
     */
    @Operation(
        operationId = "navigate",
        summary = "Queries all data sources to populate the associations starting from the given entry.",
        tags = { "Navigation" },
        responses = {
            @ApiResponse(responseCode = "200", description = "The map of the references by the URI of association meta we passed in the associations parameter.", content = {
                @Content(mediaType = "application/json", array = @ArraySchema(schema = @Schema(implementation = NavigationResponse.class)))
            })
        }
    )
    @RequestMapping(
        method = RequestMethod.POST,
        value = "/navigate",
        produces = { "application/json" },
        consumes = { "application/x-www-form-urlencoded" }
    )
    
    default ResponseEntity<List<NavigationResponse>> navigate(
        @Parameter(name = "objectUri", description = "The URI of the api object that is the starting point of the navigation. It must be a valid URI that can be the starting point of the associations we provided.", required = true) @Valid @RequestParam(value = "objectUri", required = true) URI objectUri,
        @Parameter(name = "associationMetaUris", description = "The list of associations to identify the direction we want to navigate. If we skip this parameter (null) then we will have all the associations defined in the meta") @Valid @RequestPart(value = "associationMetaUris", required = false) List<URI> associationMetaUris
    ) throws Exception {
        return getDelegate().navigate(objectUri, associationMetaUris);
    }

}
